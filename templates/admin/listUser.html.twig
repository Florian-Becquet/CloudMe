<div class="col-10 ml-auto mr-auto">
    <i id="loupeDisplay" class="fas fa-search mt-2 mb-2" aria-hidden="true"></i>
    <table class="table table-striped table-hover table-bordered">
        <thead>
            <tr id="liste">
                <th scope="col" data-search="firstName">Prénom</th>
                <th scope="col" data-search="name">Nom</th>
                <th scope="col" data-search="email">Email</th>
                <th scope="col" data-search="status">Status</th>
                <th scope="col" >Souscriptions</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr>
                    <td>{{ user.firstName }}</td>
                    <td>{{ user.name }}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.status }}</td>
                    <td>{{ user.count }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
     {{ knp_pagination_render(users) }}
</div>
<script src="js/actif.js"></script>
<script src="js/pagination.js"></script>